int minCost(List<Integer> a){
  PriorityQueue<Integer> pq=new PriorityQueue<>();
  for(int ele:a){
    pq.add(ele);
  }
  int ans=0;
  while(!pq.isEmpty()){
    int x=pq.remove();
    int y=pq.remove();
    int sum=x+y;
    ans+=sum;
    pq.add(sum);
  }
  return ans;
}
