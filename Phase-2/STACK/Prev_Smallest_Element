public PSE(arr){
  int [] ps=new int[n];
  Stack st=new Stack<>();
  Arrays.fill(ps,n);
  for(int i=n-1;i>=0;i--){
    while(!st.isEmpty() && arr[i]<arr[st.peek()]){
      ps[st.peek()]=arr[i];
      st.pop();
    }
    st.push(i);
  }
  return ps;
}
