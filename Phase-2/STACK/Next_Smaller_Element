public NSE(arr){
  int [] ns=new int[n];
  Stack st=new Stack<>();
  Arrays.fill(ns,n);
  for(int i=0;i<n;i++){
    while(!st.isEmpty() && arr[i]<arr[st.peek()]){
      ns[st.peek()]=arr[i];
      st.pop();
    }
    st.push(i);
  }
  return ns;
}
