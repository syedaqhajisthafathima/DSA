public static boolean onlyVowels(String s) {
    s = s.toUpperCase();

    for (int i = 0; i < s.length(); i++) {
        char ch = s.charAt(i);

        if (ch != 'A' && ch != 'E' && ch != 'I' && ch != 'O' && ch != 'U') {
            return false;
        }
    }
    return true;
}
